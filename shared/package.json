{
  "name": "@prova/shared",
  "version": "0.1.0",
  "description": "Shared cross-component schemas for Prova (proof I/O, IVMS101, API types, verification key).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "typescript": "~5.6.0"
  }
}
